# --- JetBrains IDEs (IntelliJ, WebStorm, etc.) ---
.idea/
*.iml
*.ipr
*.iws
out/

# --- VS Code ---
.vscode/
*.code-workspace

# --- OS cruft ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- vgs CLI / Rust build artifacts (if a local engine build lives here) ---
target/
**/target/
debug/
*.pdb

# --- Logs & temp ---
*.log
*.tmp
*.bak
*~

# NOTE: *.vgs.meta sidecars are intentionally committed — they carry stable
# GUIDs/hashes the vgs tooling needs for cross-script references. Do not ignore.
